{
  "_comment": "Manual overrides for mod-cache.json. These corrections persist across scraper updates.",
  "_docs": {
    "usage": "Add entries here to override or correct auto-generated mod data",
    "keys": {
      "_deleted": "Set to true to remove an entry from the final result",
      "_manual": "Set to true to mark entry as fully manual (scraper skips)",
      "_comment": "Documentation, stripped at runtime"
    },
    "examples": {
      "fix_field": { "displayText": "Corrected value" },
      "delete_entry": { "_deleted": true },
      "manual_entry": { "_manual": true, "text": "Custom mod" }
    }
  },
  "mods": {
  }
}
